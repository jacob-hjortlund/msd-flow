_target_: msdflow.model.NCSNpp
_partial_: true
in_channels: 1
out_channels: 1
base_channels: 128
channel_multipliers: [1, 1, 1, 2, 2, 4, 4]
num_res_blocks: 2
attn_resolutions: [16]
dropout: 0.1
num_groups: 32
num_heads: 1
activation:
  _target_: jax.nn.swish
  _partial_: true
fourier_scale: 16.0
skip_rescale: true
image_size: ${image_size}
cond_dim: "${if_cond: ${metadata_columns}, 1, 0}"
prediction_type: "velocity"
compute_dtype: "${jnp_dtype: float32}"
attention_dtype: "${jnp_dtype: float32}"
attention_implementation: null
attention_type: "dot_product"
use_coord_conv: false
